crud {
    database {
        host = "127.0.0.1"
        driver-class-name = "org.postgresql.Driver"
        database-name = "crud_db"
        user = "postgres"
        password = "postgres"
        ssl = "true"
        ssl-mode = "disable"
        connection-pool-size = "10"

        migration-config {
            migration-files-location = "db/migration"
            user = "postgres"
            password = "postgres"
        }
    }
}
